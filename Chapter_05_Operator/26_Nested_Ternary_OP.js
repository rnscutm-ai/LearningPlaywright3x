// multiple condition

let age = 26;
// age > 18 -- he will go to goa else not
// drink > 26 -- yes else no

let is_he_will_enjoy = age > 18 ? (age > 26 ? "Drink" : "No") : false;
console.log(`Can he will Drink ? : ${is_he_will_enjoy}`);

